# Tasks

- [x] Specify the overload retry contract.
- [x] Add both upstream overload codes to the existing transient retry sets.
- [x] Add regression coverage for classification, direct retry, and pre-created replay eligibility.
- [x] Run focused proxy tests, static checks, and the full test suite.
- [ ] Verify a bounded live GPT-5.6 Terra canary after deployment.

OpenSpec CLI validation was attempted but the executable is not installed in
this checkout; repository tests and static checks passed.
